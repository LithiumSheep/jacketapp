<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lithiumsheep.jacketapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--Run-time Permissions-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".JacketApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.SplashActivity"
            android:theme="@style/AppTheme.TransparentStatus" />

        <activity
            android:name=".ui.activities.SettingsActivity"
            android:label="@string/preferences" />

        <activity android:name=".experimental.TransitionActivity" />
        <activity android:name=".experimental.TransitionBActivity" />

        <activity android:name=".experimental.MultiTransitionActivity" />
        <activity android:name=".experimental.MultiTransitionEndActivity" />
        <activity android:name=".experimental.BottomSheetActivity" />

    </application>

</manifest>